import {Container} from "@mui/material";
import {Helmet} from "react-helmet";

const Blogs = () => {

    return (
        <Container>
            <Helmet>
                <title>Blogs</title>
            </Helmet>

            <div>Blogs</div>
        </Container>
    )
}

export default Blogs
