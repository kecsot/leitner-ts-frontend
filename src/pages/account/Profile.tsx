import {Container} from "@mui/material";
import {Helmet} from "react-helmet";

const Profile = () => {

    return (
        <Container>
            <Helmet>
                <title>Profile</title>
            </Helmet>

            <div>Profile</div>
        </Container>
    )
}

export default Profile;